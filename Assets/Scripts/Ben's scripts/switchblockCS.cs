using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class switchblockCS : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        gameObject.layer = 0;
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    

}
